(debug)
(if (contains (window_name) "RealTimeBattle")
  (begin (geometry "480x157+0+0")
         (below)))
(if (contains (window_name) "Arena")
  (begin (geometry "544x584+480+0")
         (above)))
(if (contains (window_name) "Messages")
  (begin (geometry "480x238+0+0")
         (above)))
(if (contains (window_name) "Score")
  (begin (geometry "480x402+0+238")
         (above)))
