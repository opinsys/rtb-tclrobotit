(debug)
(if (is (window_name) "RealTimeBattle")
  (begin (geometry "480x157+0+0")
         (below)))
(if (contains (window_name) "Arena")
  (begin (geometry "475x544+546+0")
         (above)))
(if (contains (window_name) "Messages")
  (begin (geometry "1021x232+0+504")
         (below)))
(if (contains (window_name) "Score")
  (begin (geometry "544x179+0+397")
         (above)))
(if (is (window_name) "rtbtcl")
  (begin (geometry "544x369+0+0")
         (above)))
